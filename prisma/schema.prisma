// 这是 Prisma 的核心配置文件

// 1. 告诉 Prisma 生成 JavaScript 客户端，让 index.js 能用
generator client {
  provider = "prisma-client-js"
}

// 2. 告诉 Prisma 连接到 PostgreSQL 数据库
// 它会自动读取你在 Render 环境变量里填写的 DATABASE_URL
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// --- 注意 ---
// 下面通常需要定义你的数据表（比如 User, Chat 等）。
// 因为我没看到你的 index.js 代码，不知道你需要存什么数据，
// 所以这里先留空。但这已经足够让你通过 "build" 阶段了！